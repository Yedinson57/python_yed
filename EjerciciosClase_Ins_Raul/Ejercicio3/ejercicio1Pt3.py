# creamos la lista de numero reales
numeros = [4, 2, 5, 3]

# Suma total dividida entre la cantidad de elementos
media = sum(numeros) / len(numeros)

# imprimimos el resultado
print("La media es:", media) # La media es: 3.5